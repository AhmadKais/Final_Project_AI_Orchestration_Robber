# Gmail OAuth secrets -- NEVER commit (spec Appendix E rules 39-40)
credentials.json
token.json

# Local secrets (e.g. a GitHub PAT for pushing) -- NEVER commit
.env

# ngrok authtoken -- NEVER commit (same rationale as the Gmail secrets above)
tools/ngrok.yml

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
dist/
build/

# Game artifacts (per-team, per-match; not source)
logs/*.json
reports/*.json
!logs/.gitkeep
!reports/.gitkeep

# Editors / OS
.vscode/
.idea/
.DS_Store

# Claude Code local tool settings (machine-specific, not project source)
.claude/

# Translation working files (not part of the code submission)
translation_chunks/
police_thief_p2p_he.txt

# Third-party binary, not source -- downloaded on demand for Stage 5 (Cloud
# Exposure and Tunneling), see docs/TUNNELING.md
tools/ngrok
